spring:
  profiles.active: cloud
---
spring:
  profiles:
    name: cloud
  application:
    name: gateway
  cloud:
    config:
      uri: http://config-99.cfapps.io
encrypt:
  failOnError: false
---
spring:
  profiles:
    name: development
  application:
    name: gateway
  cloud:
    config:
      uri: http://configserver:8888
encrypt:
  failOnError: false
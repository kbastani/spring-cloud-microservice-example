spring:
  profiles:
    active: cloud

---

spring:
  profiles: cloud
  application:
    name: moviesui
  cloud:
    config:
      uri: http://config-99.cfapps.io/
encrypt:
  failOnError: false

---

spring:
  profiles: development
  application:
    name: moviesui
encrypt:
  failOnError: false